<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
   <body>
     <h1>To Do List</h1><hr>
      <% if(todos.length === 0){ %>
				<h3>There are no To Do's yet!</h3><hr>
    			<% } else { %>
						<ul class="listUL"></ul>
							<% todos.forEach((todo) =>{ %>
								<li class="listLI">
									<%= todo.title %> | <%= todo.done %>
								</li>
							<% }) %>
						</ul>
    			<% } %>
    	  	  <form action="/" method="post">
    		     <input type="text" class="form-control" name="todo" placeholder="Text here">
					     <button>Add To Do</button>
			    	</form>
    	</body>
</html>
